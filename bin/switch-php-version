#!/bin/sh

if [ $# = 0 ] ; then
    version=""
else
    version=@$1
fi

if [ ! -e /usr/local/opt/php$version ] ; then
    echo "No such version"
    exit
fi

ln -snf /usr/local/opt/php$version ~/.php
