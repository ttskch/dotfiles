unbind-key C-b
set-option -g prefix C-t
bind-key C-t send-prefix

unbind-key '%'
bind-key '|' split-window -h

unbind-key '"'
bind-key '-' split-window -v

# 要 brew install reattach-to-user-namespace
set-option -g default-command "reattach-to-user-namespace -l zsh"
